Place the provided collage image (the 2x3 grid of photos) here and name it `collage.jpg`.

File path expected by the site: `images/portfolio/collage.jpg`

Notes:
- The site uses this single image as a sprite and crops it into six tiles (3 columns × 2 rows).
- If your image has different aspect ratio or padding, you may need to adjust the CSS rules in `css/custom.css` under the `.portfolio-sprite` and `.slice-#` classes.
